export enum Operator {
  gt = '>',
  gte = '>=',
  eq = '=',
  ne = '!=',
  is = '!!',
  isNot = '!',
  lt = '<',
  lte = '<=',
  regex = '~',
  notRegex = '!~',
  between = '><',
  notBetween = '>!<',
  in = '<>',
  notIn = '<!>',
}
